#
# Copyright (c) 2013 Shotgun Software, Inc
# ----------------------------------------------------
#
# Metadata defining the behaviour and requirements for this app

# expected fields in the configuration file for this app
configuration:

    # general settings
    template_work:
        type: template
        required_fields: [version]
        optional_fields: [name]
        description: A reference to a template which locates a work file on disk.
        
    template_snapshot:
        type: template
        required_fields: [version]
        optional_fields: [name, timestamp, increment]
        description: A reference to a template which defines the location where snapshot
                     backups will be stored on disk.

    hook_scene_operation:
        type: hook
        parameters: [operation, file_path]
        default_value: "scene_operation_{engine_name}"
        description:    
        
    hook_copy_file:
        type: hook
        parameters: [source_path, target_path]
        default_value: copy_file
        description: Specify a hook that will be used to copy the file 'source_path' 
                     to 'target_path'.
    
    # snapshot settings
    snapshot_display_name:
        type: str
        default_value: Snapshot Work File
        description:
        
    hook_thumbnail:
        type: hook
        parameters: []
        default_value: thumbnail
        description: Specify a hook to pre-generate a thumbnail for the snapshot.

    # snapshot history settings: 
    snapshot_history_display_name:
        type: str
        default_value: Work File Snapshot History
        
            
# the Shotgun fields that this app needs in order to operate correctly
requires_shotgun_fields:

# More verbose description of this item 
display_name: "Snapshot"
description: "File snapshot functionality."

# Required minimum versions for this item to run
requires_shotgun_version:
requires_core_version: "v0.12.21"
requires_engine_version: 

# the engines that this app can operate in:
supported_engines: [tk-nuke, tk-maya]

# the frameworks required to run this app
frameworks:
- {"name": "tk-framework-widget", "version": "v0.1.10"}
    
